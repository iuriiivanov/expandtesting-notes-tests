# API Configuration
API_BASE_URL=""

# Test User Credentials (optional, used for manual/debug scenarios)
# The test framework auto-creates users via fixtures, so these are optional
TEST_USER_EMAIL="test@example.com"
TEST_USER_PASSWORD="TestPass123!"
TEST_USER_NAME="Test User"

# ⚠️ Do NOT hardcode auth tokens here — they expire!
# Token is obtained dynamically via POST /users/login
